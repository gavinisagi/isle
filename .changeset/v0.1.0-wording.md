---
---

docs: soften the README roadmap wording — `v0.1.0` is an internal milestone tag, not a published release. Drops the "shipped"/release-link framing. Docs only, no package version change — empty changeset. / 软化 README 路线图措辞:`v0.1.0` 是内部里程碑 tag,非对外发布;去掉 "shipped"/release 链接。纯文档,空 changeset。
